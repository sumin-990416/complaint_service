{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "uv run uvicorn backend.main:app --host 0.0.0.0 --port 8000",
    "restartPolicyMaxRetries": 5
  },
  "plugins": ["postgresql"]
}
